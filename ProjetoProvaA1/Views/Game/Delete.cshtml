@model ProjetoProvaA1.Models.Game

@{
    ViewBag.Title = "Delete";
}

<h2 style="color: #1E90FF; font-weight: bold;">Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4 style="color: #1E90FF;">Game</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Developer.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Developer.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.ReleaseDate)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ReleaseDate)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-secondary", style = "margin-left: 10px;" })
        </div>
    }
</div>

<style>
    /* Título */
    h2 {
        color: #1E90FF;
        font-weight: bold;
    }

    /* Cabeçalho da seção */
    h4 {
        color: #1E90FF;
    }

    /* Botões */
    .btn-danger {
        background-color: #dc3545;
        color: white;
        border: none;
        padding: 10px 20px;
        font-size: 16px;
    }

    .btn-secondary {
        background-color: #6c757d;
        color: white;
        border: none;
        padding: 10px 20px;
        font-size: 16px;
    }

    /* Margem entre os botões */
    .col-md-offset-2 .btn-secondary {
        margin-left: 10px;
    }
</style>
